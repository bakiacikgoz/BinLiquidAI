# Spike Bridge (Research-only)

This directory is intentionally isolated from product runtime.
It provides a toy spike-to-vector bridge for experimental workflows.

Run:

```bash
uv run python research/spike_bridge/run_toy_bridge.py --bits 16 --seed 42
```
