{
  "$id": "https://schemas.imperaos.local/artifacts/artifact-duplicate-command.v1.schema.json",
  "additionalProperties": false,
  "properties": {
    "artifactId": {
      "anyOf": [
        {
          "maxLength": 128,
          "minLength": 1,
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Artifactid"
    },
    "idempotencyKey": {
      "maxLength": 128,
      "minLength": 1,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$",
      "title": "Idempotencykey",
      "type": "string"
    },
    "sourceArtifactId": {
      "maxLength": 128,
      "minLength": 1,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$",
      "title": "Sourceartifactid",
      "type": "string"
    },
    "title": {
      "maxLength": 200,
      "minLength": 1,
      "title": "Title",
      "type": "string"
    }
  },
  "required": [
    "sourceArtifactId",
    "title",
    "idempotencyKey"
  ],
  "title": "DuplicateArtifactCommand",
  "type": "object",
  "x-imperaos-contract": "artifact-duplicate-command.v1"
}